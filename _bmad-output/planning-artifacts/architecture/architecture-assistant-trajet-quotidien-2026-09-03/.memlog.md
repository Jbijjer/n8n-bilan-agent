---
scope: Système complet (v1)
purpose: Build substrate pour bmad-spec / epics / stories
altitude: initiative
updated: 2026-09-03T14:59
---

- (event) Mode=coaching. Livrable=spine seul (bmad-architecture ARGUMENTS: 'pas de document').
- (direction) Paradigme/orchestration = décision load-bearing la plus structurante. Vérification web faite (n8n AI Agent = chat+mémoire+tools, pas de FSM native ; LangGraph v1.1.6 Apache 2.0, adoption prod réelle Klarna/LinkedIn/Replit/Uber/GitLab, state machine native via nodes/edges/checkpointers). 3 alternatives présentées à Sébastien : n8n, OpenClaw, Python custom+LangGraph (nouvelle option, pas dans le brief original).
- (decision) AD candidat 1 (paradigme) : Python + LangGraph, confirmé par Sébastien. Raison : seule option garantissant le séquencement strict (FR-4/5/6) par construction (FSM native) plutôt que par la qualité du prompt (n8n et OpenClaw échouent tous deux ce test).
- (decision) AD candidat : checkpointer LangGraph scope par flux (clé chat_id+date+type_flux), backend SQLite. Continuité inter-flux exclusivement via la fiche du jour (jamais via le checkpointer) — applique FR-8/FR-9 par construction. Expliqué en détail à Sébastien (analogie sauvegarde/reprise).
- (decision) AD candidat : STT (faster-whisper) et TTS (Piper) in-process (librairies Python), pas de micro-service HTTP séparé — confirmé par Sébastien. Addendum du brief mis à jour en conséquence (section Vocal, Orchestration barrée).
- (decision) AD candidat : appel LLM isolé derrière une interface unique LLMClient, un adaptateur par fournisseur (Ollama/GPT-4o compatibles OpenAI via même adaptateur ; Claude nécessite un adaptateur dédié, API non compatible OpenAI). Vérifié : python-telegram-bot v22.8 (webhook natif via Starlette/Uvicorn), Ollama expose /v1/chat/completions compatible OpenAI, faster-whisper conçu pour usage in-process.
- (constraint) Topologie matérielle révélée par Sébastien : deux hôtes distincts — Unraid (Docker, orchestrateur) et une machine séparée avec GTX 5060 16GB (GPU). Communication via Tailscale (réutilise contrainte non négociable n°5).
- (decision) AD révisé : appel LLM isolé via LiteLLM (proxy self-hosted sur machine GPU, endpoint unique compatible OpenAI, unifie Ollama+Claude+GPT-4o). Remplace l'AD précédent (adaptateur par fournisseur dans l'orchestrateur) — plus simple, zéro changement de code côté orchestrateur pour swap de modèle. Vérifié : LiteLLM v1.99.0, actif ; épingler à ≥1.83.0 (incident supply-chain mars 2026, corrigé).
- (decision) AD révisé (correction) : STT (faster-whisper) n'est PAS in-process sur l'orchestrateur — tourne comme service réseau sur la machine GPU (large-v3 a besoin du GPU). TTS (Piper) reste in-process sur Unraid (CPU, léger). Si upgrade vers Coqui XTTS v2 (vérifié : fork maintenu idiap/coqui-ai-TTS depuis fermeture Coqui AI janvier 2024, licence CPML non-commerciale sans incidence pour usage perso) : devra tourner sur machine GPU — Deferred.
- (decision) Déploiement : Docker sur Unraid pour l'orchestrateur (confirmé par Sébastien, remplace ma proposition systemd).
- (decision) AD confirmé (accepté implicitement) : écriture atomique de la fiche du jour — toute exception avant l'écriture finale déclenche notification Telegram, jamais d'échec silencieux ni de fiche partielle non signalée (applique FR-13/14/15).
- (decision) AD confirmé (accepté implicitement) : un seul module possède le vault Obsidian (lecture/écriture de la fiche du jour) — aucun node LangGraph ne touche le système de fichiers directement.
- (decision) AD confirmé (accepté implicitement) : transport Telegram via python-telegram-bot v22.8 en mode webhook (cohérent avec Cloudflare Tunnel déjà décidé), vérification chat_id (FR-1) en amont de toute logique de flux.
- (assumption) Temps de réponse (§5 PRD) : Deferred — instrumentation dès le premier déploiement, mesure empirique pendant la séquence de tests déjà prévue au brief, pas de borne chiffrée fixée maintenant.
- (event) Spine distillé : 7 AD (dépendance, checkpointer, continuité, LLM/LiteLLM, STT/TTS split, écriture atomique, frontière transport), paradigme hexagonal, stack vérifiée (LangGraph 1.1.6, ptb 22.8, LiteLLM 1.99.0/pin>=1.83.0, Ollama 0.33.x, faster-whisper 1.2.1, Piper — fork actif OHF-Voice/piper1-gpl 1.6.0 GPL-3.0, rhasspy/piper MIT archivé oct 2025). Topologie 2 hôtes en diagramme mermaid. 7 items Deferred.
- (event) lint_spine.py : 6 findings low, tous faux positifs (notation f-string thread_id confondue avec placeholder de template). Ignorés sciemment.
